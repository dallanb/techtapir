apiVersion: v1
kind: Service
metadata:
  labels:
    app: account
  name: account
  annotations:
    konghq.com/plugins: kong-jwt
spec:
  ports:
  - port: 50000
    protocol: TCP
    targetPort: 5000
  selector:
    app: account
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth
  name: auth
spec:
  ports:
  - port: 50001
    protocol: TCP
    targetPort: 5000
  selector:
    app: auth
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: contest
  name: contest
  annotations:
    konghq.com/plugins: kong-jwt
spec:
  ports:
  - port: 50002
    protocol: TCP
    targetPort: 5000
  selector:
    app: contest
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: course
  name: course
  annotations:
    konghq.com/plugins: kong-jwt
spec:
  ports:
  - port: 50003
    protocol: TCP
    targetPort: 5000
  selector:
    app: course
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: league
  name: league
  annotations:
    konghq.com/plugins: kong-jwt
spec:
  ports:
  - port: 50004
    protocol: TCP
    targetPort: 5000
  selector:
    app: league
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: static
  name: static
spec:
  ports:
  - port: 40000
    protocol: TCP
    targetPort: 80
  selector:
    app: static
---