version: '3.2'
services:
  nfs:
    container_name: nfs
    image: itsthenetwork/nfs-server-alpine:$NFS_VERSION
    restart: unless-stopped
    privileged: true
    environment:
      - SHARED_DIRECTORY=/data
    volumes:
      - $HOME/nfs.:/data
    ports:
      - 2049:2049
    networks:
      - nfs-net
networks:
  nfs-net:
    driver: bridge